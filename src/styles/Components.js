import styled from 'styled-components';
import {motion} from 'framer-motion'
export const Settings = styled(motion.button)`
  border: none !important;
  background: transparent !important;
  box-shadow: none !important;
`;
